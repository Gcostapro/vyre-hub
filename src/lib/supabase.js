// Supabase client — configure com suas credenciais quando pronto
// import { createClient } from "@supabase/supabase-js";
// export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);

export const supabase = null; // usando dados mock por enquanto
